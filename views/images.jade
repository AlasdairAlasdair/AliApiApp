extends layout

block content
  .container
    h1= title
    p Welcome to #{title}
    .row
      - each post in pageData
        .col-md-2
          a(href=post.source): img.img-responsive(src=post.thumbnail, alt=post.title)